services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: supabase-probe-app
    ports:
      - "5555:3000"
    environment:
      PORT: "3000"
      DATABASE_URL: "postgresql://postgres:AXxH23D1kJuuKanrGeYeN3AChXqm4hCI@host.docker.internal:5432/postgres"
      DB_SSL: "false"
      DB_SSL_REJECT_UNAUTHORIZED: "true"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
